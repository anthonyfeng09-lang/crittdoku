# Supabase config for production builds (npm run build).
#
# These are safe to commit: the URL is public, and the publishable key is
# meant to ship in the browser bundle - every row is guarded by the
# row-level-security policies in SUPABASE.md, so the key alone grants
# nothing without a signed-in user. Rotate in the Supabase dashboard if
# needed and update here.

VITE_SUPABASE_URL=https://oorlbmbonftdswvcnfmc.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_intNkyNhybkv3HRYh44ueQ_OpfZJMwH
